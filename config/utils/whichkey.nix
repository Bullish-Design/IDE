{
  # TODO: Implement general mappings
  plugins.which-key = {
    enable = true;
    ignoreMissing = true;
    icons = {
      breadcrumb = "»";
      group = "+";
      separator = ""; # ➜
    };
    
    # Registrations:
    # registrations = {
    #   "<leader>t" = " Terminal";
    # };


    window = {
      border = "rounded";
      winblend = 0;
    };
  };
}
